# Queues

<!-- toc -->

```yaml

name: TOPOLOGY-NAME

queues:
  queue1:
    function: function1
    functions:
      - function1
      - function2
```

By default, tc uses `sqs` as the queue.

## Composition
